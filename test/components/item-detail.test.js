import ItemDetail from '../../src/components/item-detail';
import { renderComponent  } from '../test_helper';

describe('ItemDetail' , () => {
  let component;

  beforeEach(() => {
    component= renderComponent(ItemDetail);
  });

});
